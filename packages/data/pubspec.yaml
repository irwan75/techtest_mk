name: data
version: 1.0.0+1
publish_to: none
description: Data Packages.
environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: "2.10.5"

dependencies:
  network:
    path: ../network
  shared:
    path: ../shared
  maukerja_techtest:
    path: ../../

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.10
  json_serializable: ^6.2.0

flutter:
  uses-material-design: true
